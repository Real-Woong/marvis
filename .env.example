TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: proactive reminders use the chat ID saved by /start when omitted.
TELEGRAM_CHAT_ID=
# Optional: enables the local Siri Shortcut capture webhook (127.0.0.1:8081)
# when set. Generate with: openssl rand -hex 32
SIRI_SHORTCUT_SECRET=
