# 2026-09-02 · 바닥부터 다시

하루에 12커밋. 저장소, 처리 경로, LLM 계층, 배포까지 전부 갈아엎었다.

## 무엇이 문제였나

1. **JSON 파일 저장소.** 자물쇠를 걸어 급한 불은 껐지만, 파일 전체를 매번
   다시 쓰는 구조는 그대로였다. 히스토리가 없어 "언제 왜 사라졌나"를 되짚을
   수 없었고, 항목 번호가 저장할 때마다 다시 매겨져 `/done 12`가 어제와
   오늘 다른 것을 가리켰다.
2. **처리 경로가 둘.** 텔레그램(`handlers.py`)과 Siri 웹훅(`webhook.py`)이
   같은 흐름을 각자 구현했고 이미 어긋나 있었다 — 웹훅에는 프로젝트 명령도
   `!스케쥴`도 없었다.
3. **정규식의 한계.** 문장을 정규식으로 훑어 의도를 맞히는 방식은 늘어날수록
   서로를 밟았다. LLM에게 넘기고 싶지만, 잘못 넘기면 사용자 데이터가 걸린다.

## 무엇을 바꿨나

**SQLite** (`ee47968`, `3f5abb9`)
`marvis/db.py` + `migrate.py`. 항목마다 영구 UUID와 사용자에게 보이는 고정
번호(`seq`)를 준다. 한 번 매긴 번호는 절대 바뀌지 않으므로 `/done 12`는 언제
실행해도 같은 것을 가리킨다. 지난 일정을 **지우지 않고 보관**(archived)한다.
모든 쓰기는 이벤트 로그를 남긴다 — 나중에 "누가 이걸 지웠나"를 답할 수 있는
유일한 근거가 됐다.

**단일 처리 경로** (`fe67c4e`)
`core.py` 하나가 무엇을 할지 정하고, 어댑터는 "어떻게 보낼지"만 안다.
텔레그램이든 Siri든 같은 판단을 거친다.

**도구 표면과 LLM 계층** (`ead2acc`)
`llm/tools.py`에 "저장/조회/삭제/프로젝트 갱신"을 도구로 정의하고, 프로바이더에
중립인 클라이언트 인터페이스를 뒀다(Gemini / Anthropic 교체 가능). 도구마다
쓰기 여부를 표시해 dry_run으로 막을 수 있게 했다.

**에이전트 루프 + shadow 라우터** (`348da99`)
모델이 도구를 골라 부르는 루프를 넣되, 기본 모드를 `shadow`로 뒀다.

    regex   정규식만. 기존 동작.
    shadow  정규식이 사용자에게 응답하고, LLM은 나란히 돌며 로그에만 남는다.
    llm     LLM이 응답한다.

배포해도 사용자 경험이 그대로이고, 그동안 쌓이는 불일치 기록이 골든셋 재료가
된다. 되돌리려면 환경변수 하나만 바꾸면 된다. **한 번에 갈아타지 않은 것이
이 날의 가장 잘한 판단이었다.**

**평가 하네스** (`465d156`)
`evals/golden.jsonl`에 실제 발화 35건과 정답을 넣고 회귀를 잰다. 라우터를
바꿀 때 "느낌상 나아졌다" 말고 숫자를 보기 위해서.

**초기화 CLI** (`da38a1f`, `2acde29`)
데이터를 진짜로 지우는 경로를 따로 만들었다. 지운 건수를 세기 전에
마이그레이션을 먼저 돌리지 않으면 "0건 지웠다"고 거짓말을 한다.

**맥미니 이전** (`a6994d3` 외 3건)
Oracle Cloud VM(systemd)에서 집에 있는 맥미니(launchd LaunchDaemon)로 옮겼다.
로그인하지 않아도, 재부팅 직후에도 뜬다. 죽으면 다시 띄우되 크래시 루프가
CPU를 태우지 않게 재시작 간격을 뒀다.

이전 첫날에 세 번 넘어졌다: 스크립트를 다른 위치에서 실행하면 검증이 깨지고
(`9d624a6`), 키 없이 실행하면 빈 배열 확장에서 죽고(`6fd5520`), 데몬 재시작
경합과 웹훅 바인딩 실패로 봇이 통째로 죽었다(`74a8e85`).

## 남은 것

- shadow 모드의 불일치 기록을 아직 충분히 모으지 못했다. `llm` 전환 시점은
  그 기록을 보고 정한다.
- shadow가 관찰이 아니라 실행이 되어 하루에 89건을 저장한 사고가 이 구조에서
  났다(2026-09-04에 `dry_run=True`로 못박음).
