# 2026-05 · 만들기 시작

## 왜 만들었나

아이디어가 떠올라도 적을 곳이 마땅치 않아 흘려보냈고, 일정 관리 앱은 켜는 것
자체가 일이었다. 이미 하루 종일 켜 두는 창구가 텔레그램이라, 거기에 말만
던지면 알아서 기억해 주는 것을 원했다.

이름은 "My + Jarvis = Marvis".

## 무엇을 만들었나

**2026-05-15 · 첫 커밋** (`401137d`, `d7ad8aa`)
`bot.py` 한 파일 152줄. 텔레그램 메시지를 받아 Gemini에게 넘기고, 답을 gTTS로
한국어 mp3로 만들어 텍스트와 함께 돌려줬다. 이동 중에 에어팟으로 듣자는 게
음성의 이유였다.

**2026-05-24 · 기억과 일정** (`dac82c3`, `6fb9bf2`)
말한 것을 JSON 파일에 쌓기 시작했다. 문장을 정규식으로 훑어 일정·아이디어·메모로
나누고, "내일", "3시" 같은 표현에서 날짜와 시각을 뽑았다.

**2026-05-29 · 알림** (`3caca93`)
저장만으로는 부족했다. 저장한 시각이 되면 먼저 말을 걸어야 비서다. 백그라운드
스레드가 30초마다 저장소를 훑어 발송 시각이 지난 항목을 텔레그램으로 보낸다.
지금도 같은 구조다(`marvis/reminders.py`).

이 시점의 `bot.py`는 422줄이 더해져 700줄에 가까웠다. 모든 것이 한 파일에 있었다.

## 남은 것 (그때는 몰랐던)

- 저장소가 JSON 파일이라, 알림 루프와 메시지 핸들러가 같은 파일을 동시에 쓰면
  한쪽이 통째로 날아갈 수 있었다 → 2026-07-28에 터졌다.
- 항목 번호를 저장할 때마다 다시 매겼다. "12번 완료"가 어제와 오늘 다른 것을
  가리켰다 → Phase 0에서 UUID + 고정 seq로 바꿨다.
